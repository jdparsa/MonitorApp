import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.PrivacyMonitor.app',
  appName: 'PrivacyMonitor',
  webDir: 'dist\PrivacyMonitor\browser'
};

export default config;
